JavaScript React "storybook" "addon-actions" Demo
================================================

'action'可以让我们方便的在storybook中记录下触发的action。

注意：某些浏览器插件可能与其冲突，如果看不到效果，检查一下console

```
npm install
npm run storybook
```
